import React from 'react'
import ReactDOM from 'react-dom/client'
import PrayerFeedApp from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PrayerFeedApp />
  </React.StrictMode>,
)
